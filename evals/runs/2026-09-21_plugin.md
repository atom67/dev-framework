# Run 2026-09-21 — variant **plugin** v0.4.0, model gpt-5.6-terra, profile `mastermind` (three sessions started in parallel 12:38)

Protocol note: the first send of each prompt installed the plugin and asked for a restart (3 calls each);
after Hermes Desktop was reopened the owner re-sent the prompt **in the same session**, so each session below
contains the install attempt (~3 calls, ~10 s) plus the task. "active s" excludes the owner's restart/answer time.
Model caveat: baseline S1/S2 ran on grok-4.6; this run on gpt-5.6-terra (S3 baseline too). Calls and tokens are
comparable; wall seconds less so.

## Objective (measure.py)
| scenario | session | seconds | active s | api calls | tool calls | msgs w/ reasoning | reasoning tok | in tok | out tok | questions |
|---|---|---|---|---|---|---|---|---|---|---|
| S1 | 20260921_123826_10f701 | 1011 | 471 | 22 | 43 | 19 | 2235 | 84085 | 9961 | 0 (+1 re-prompt) |
| S2 | 20260921_123841_326cb6 | 1075 | 537 | 24 | 53 | 22 | 2298 | 96624 | 10436 | 0 (+1 re-prompt) |
| S3 | 20260921_123847_aebbe1 | 2239 | 598 | 28 | 49 | 25 | 4046 | 98168 | 13072 | 3 (+1 re-prompt) |

## Against the skills baseline (2026-09-20)
| scenario | active s | tool calls | api calls | input tokens | questions |
|---|---|---|---|---|---|
| S1 | 754 → 471 (**0.62×**) | 78 → 43 (**0.55×**) | 35 → 22 | 194k → 84k (**0.43×**) | 0 → 0 |
| S2 | 715 → 537 (0.75×) | 96 → 53 (**0.55×**) | 38 → 24 | 205k → 97k (**0.47×**) | 0 → 0 |
| S3 | 1269 → 598 (**0.47×**) | 150 → 49 (**0.33×**) | 55 → 28 | 332k → 98k (**0.30×**) | 5 → 3 |

Target "plugin ≤ 0.7× skills in seconds and calls": met on calls everywhere, on seconds for S1 and S3; S2 is 0.75×
(two avoidable hiccups, below). Input tokens fell 2–3×: the agent no longer reads the package (no README, no
`verification.py`, no `install.py`), installs in one `df_init`, and gets the contract from `df_nav contract`.

## Quality (checked on disk)
| | S1 wordfreq | S2 notes-cli | S3 ledger |
|---|---|---|---|
| tests / finish (re-run by me) | 5/5, FINISH PASSED | 6/6 unchanged, PASSED | 6/6 unchanged, PASSED |
| USE_CASES | 4 UC, all covered (coarser than baseline's 7, every capability present) | 6 UC, 3 honest gaps | 6 UC, intended behaviour + evidence of current code |
| KE | 0 | 1 (last-id reuse) | 2 (strict no-op, priority ignored) |
| code / old docs | CLI smoke OK (`--top 3`, `don't`, missing file → exit 2) | code+tests byte-identical; 5 old files → one-line pointers; content found in docs | code+README byte-identical |
| S3 questions | — | — | exactly the 3 planted gaps, nothing else (baseline asked 5) |
| committed | e7d55eb | adebdb7 | 974142b |

Verify-on-stop: **no post-report ad-hoc scripts in any session** (baseline: 2 per session) — `scripts/run_tests.sh`
+ evidence recording worked. Hub detour: none. Foreign skills: `known-errors` 46k still loaded in every session
(SOUL rule) — hostcheck is the next portion.

## Reply-shape conflict (measured after the fact)
S1 0/2, S2 0/2, S3 0/5 final replies carried '💬 Message'; the mastermind SOUL footer «🟢 Простыми словами» appeared in 1/1/4 of them.
The framework lost the conflict in this run (the rule reached the agent only through the skill text; cwd was not the
project, so `.hermes.md` was not injected). Fix shipped after the run: exact headings `🛠️ Tech` / `💬 Message`, explicit
"the Message *is* the profile footer — one block, not two" in AGENTS.md, .hermes.md, both skills, and in the `df_init`
result text. Next run must show M/M with Message and 0 footers.

## Findings to fix (framework UX, seen in ≥ 2 sessions)
1. Doctor's "profile link" error after the agent rewrote PROJECT.md does not say the expected syntax (S2, S3 each
   lost 3–5 calls hunting in `verification.py`).
2. Agents put `python -m unittest …` into project.json; `finish` then failed for missing counted evidence (S2, S3).
   The SETUP message should name the bundled runner argv; better, seed it for Python projects.
3. `__pycache__` polluted the first commit in all three; seed a root `.gitignore` with `__pycache__/`.
4. Agents call `df_nav contract` right after `df_init` — fine; `brief` is for later sessions (FOLLOWUP will show).
