---
description: Install the DEV Framework into a repository (documents + verification gates), then fill its project facts (alias of the df-init skill; kept for one release)
argument-hint: <target-repo-path> [project name] [scale, e.g. "100 users"]
allowed-tools: Skill
---

Invoke the skill `dev-framework:df-init` with these arguments and follow it: $ARGUMENTS
