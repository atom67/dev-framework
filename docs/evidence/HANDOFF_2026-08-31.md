# Handoff pilot — 2026-08-31

Status: fixture verified; native fresh-session assessment BLOCKED by environment/access.
Not a completed cross-provider compatibility certification.

## Reproduction and scope

Prepare with [handoff_pilot.py](../../scripts/handoff_pilot.py). It creates a fresh synthetic
Parcel Ledger project under an OS temporary directory, installs the framework, initializes
Git without commits, sets real Python tests and stores a durable plan/decision/next step.
No Main OS profile, source code, credentials or private user records are supplied to models.
The installed generalized lessons contain only the package's already-reviewed descriptions.

Expected recovered facts: project Parcel Ledger; branch main without commits; confirmed-ID
subtraction preserving input order (ADR-01); old clear-all defect; three recorded passing
tests/zero skips; pure-function coverage only; next repeated-acknowledgement regression;
no acceptance or authorization for edits/commit/push/deploy/restart during this read-only test.
The prompt asks for local citations and does not reveal these expected facts.

## Actual fixture evidence

- Python 3.11.15, Windows. Preparation ran real checks on both behaviours.
- Historical clear-all control: 3 tests, 2 failures, finish exit 1.
- Corrected implementation: 3 tests, 0 failures/errors/skips, finish exit 0.
- Working-source SHA256:
  `c923fa067e829be084739c70b508fef33c7e7abe236b718ef2e55f9bc830a42d`.
- Digest was unchanged after both native provider attempts. No application was launched,
  no commit created and no fixture files changed by the providers.
- Root: newly created `devframework-pilot-0bsruw70/project` in the OS temp directory.
  The generator is the reproducible artifact; local scratch is not a permanent dependency.

## Native provider attempts

| Provider | Configuration | Observed result | Verdict |
|---|---|---|---|
| Codex CLI 0.151.0-alpha.7.1 | fresh exec, ephemeral, ignore user config, read-only sandbox, approval never; configured default selected gpt-5.6-sol | AGENTS.md auto-loaded (project title recognized); even Get-Content PROJECT.md rejected by execution policy; model explicitly reported missing evidence | BLOCKED: no repository handoff pass |
| Claude Code 2.1.177 | fresh print, no session persistence, Read/Glob/Grep only, project settings, hooks/memory disabled, no MCP servers | 401 expired OAuth credential before model turn; reported 0 input/output tokens and 0 cost | BLOCKED: reauthentication required |
| Cursor Desktop 3.17.21 | availability/help inspection only; no callable agent or cursor-agent binary found; cursor agent --help returned desktop help | No fresh headless model session executed | NOT RUN |

No permissions were bypassed, credentials read/exported, account reset performed or provider
installed. Request for Claude reauthentication was sent to the operator. Do not weaken
machine policy to make a blocked probe appear successful.

## Next authorized check

After provider access is restored, regenerate the fixture and run the same read-only prompt
in fresh sessions. Compare recovered facts with the expectations above, inspect actual
read/tool traces and compare source digests. Only then mark a native handoff pass. Full
continuation with implementation would be a separate explicitly scoped pilot step.

Invocation details were checked against local CLI help and
[OpenAI non-interactive documentation](https://learn.chatgpt.com/docs/non-interactive-mode),
[Claude CLI reference](https://code.claude.com/docs/en/cli-reference) and
[Cursor CLI installation](https://cursor.com/docs/cli/installation).
