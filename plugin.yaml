name: dev-framework
version: 1.2.0
description: "DEV Framework for Hermes: documents that agents actually read and gates that prove work is done — df_init (install into a repo), df_check (doctor / finish / commit-check / selftest / secrets), df_nav (session brief, find one block, index, checklists, handoff). Bundles the df-import and df-catch-up skills."
author: "trenthalden"
homepage: https://github.com/trenthalden/dev-framework
provides_tools:
  - df_init
  - df_check
  - df_nav
provides_hooks: []
